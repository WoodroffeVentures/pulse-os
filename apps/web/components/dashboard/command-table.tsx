import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export function CommandTable({title, rows, columns}:{title:string; rows:Record<string, any>[]; columns:string[]}){return <Card><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent><div className="overflow-auto"><table className="w-full text-sm"><thead><tr className="text-left text-secondaryText">{columns.map(c=><th key={c} className="pb-3 font-medium">{c}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i} className="border-t border-borderline"><>{columns.map(c=><td key={c} className="py-3 text-primaryText/90">{r[c.toLowerCase()] ?? r[c] ?? '—'}</td>)}</></tr>)}</tbody></table></div></CardContent></Card>}
